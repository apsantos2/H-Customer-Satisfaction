CREATE TABLE IF NOT EXISTS `mi153_customer_satisfaction_mgramos`.`DEPARTMENT` (
  `Dept_id` INT NOT NULL,
  `Dept_Name` VARCHAR(45) NULL,
  `Dept_HeadName` VARCHAR(45) NULL,
  PRIMARY KEY (`Dept_id`))
ENGINE = InnoDB;
